# Generate requirements.txt, e.g. with Docker:
# docker run --rm -v $PWD:/src -w /src python:3.12-bookworm bash \
#   -c "pip install pip-tools && pip-compile --quiet --strip-extras --no-annotate -o ./requirements.txt ./requirements.in"
-c constraints.txt

ansible==10.6.0
ansible-lint[lock]==24.7.0
jmespath==1.0.1
python-openstackclient==7.2.0
python-octaviaclient==3.8.0
s3cmd==2.4.0

{
  "warehouse-name": "{{ lakekeeper_catalog.name }}",
  "storage-credential": {
    "type": "s3",
    "aws-access-key-id": "{{ s3_access_key_id }}",
    "aws-secret-access-key": "{{ s3_access_secret }}",
    "credential-type": "access-key"
  },
  "storage-profile": {
    "type": "s3",
    "bucket": "{{ lakekeeper_catalog.storage.bucket_name }}",
    "key-prefix": "{{ lakekeeper_catalog.storage.key_prefix }}",
    "endpoint": "{{ s3_endpoint }}",
    "region": "local-01",
    "path-style-access": true,
    "sts-enabled": false,
    "flavor": "s3-compat"
  },
  "delete-profile": {
    "type": "hard"
  }
}
